---
phase: 01-core-engine-dockerized-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [go.mod, go.sum, Dockerfile, cmd/server/main.go, internal/processor/engine.go, internal/processor/config.go]
autonomous: true
requirements: [ENV-01, ENV-02, REL-03, CORE-04]
user_setup: []

must_haves:
  truths:
    - "Project initializes and resolves dependencies without errors"
    - "Multi-stage Docker build succeeds on Alpine"
    - "The application can initialize libvips and configure cache limits at startup"
  artifacts:
    - path: "go.mod"
      provides: "Dependency management"
    - path: "Dockerfile"
      provides: "Reproducible build environment"
    - path: "cmd/server/main.go"
      provides: "Application entrypoint with libvips initialization"
  key_links:
    - from: "Dockerfile"
      to: "vips-dev"
      via: "apk add"
---

<objective>
Setup the foundational build environment and project skeleton for the Gatuno service. This includes initializing the Go module, defining the directory structure, and creating a multi-stage Dockerfile that correctly handles libvips dependencies on Alpine Linux. It also establishes the initial application entrypoint where libvips cache limits are configured.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-dockerized-build/01-core-engine-dockerized-build-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and folder structure</name>
  <files>go.mod, cmd/server/main.go, internal/processor/engine.go, internal/processor/config.go, pkg/README.md</files>
  <action>
    - Initialize Go module `go mod init gatuno`.
    - Create the following directory structure: `cmd/server/`, `internal/processor/`, `pkg/`.
    - Create placeholder files for `internal/processor/engine.go` and `internal/processor/config.go`.
    - Per D-01.
  </action>
  <verify>
    <automated>go mod edit -json</automated>
  </verify>
  <done>Go module initialized and core directories created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement multi-stage Dockerfile with libvips on Alpine</name>
  <files>Dockerfile</files>
  <action>
    - Create a multi-stage `Dockerfile`:
      - Stage 1 (builder): Use `golang:1.23-alpine3.20`. Install `vips-dev`, `build-base`, `pkgconfig`.
      - Stage 2 (runner): Use `alpine:3.20`. Install `vips` (runtime only), `ca-certificates`.
    - Ensure `CGO_ENABLED=1` for the build.
    - Copy the binary and set the entrypoint.
    - Per D-02.
  </action>
  <verify>
    <automated>docker build -t gatuno-test .</automated>
  </verify>
  <done>Dockerfile successfully builds the application in a minimal Alpine environment with libvips.</done>
</task>

<task type="auto">
  <name>Task 3: Implement libvips initialization and cache limits</name>
  <files>cmd/server/main.go, internal/processor/config.go</files>
  <action>
    - In `internal/processor/config.go`, implement an `InitVips` function that:
      - Sets `libvips` cache limits using `bimg.VipsCacheSetMax(0)` and `bimg.VipsCacheSetMaxMem(0)` (or other sensible values as per D-03).
      - Logs the `libvips` version.
    - In `cmd/server/main.go`, call `processor.InitVips()`.
    - Per D-03.
  </action>
  <verify>
    <automated>go run cmd/server/main.go | grep -i "vips"</automated>
  </verify>
  <done>Application successfully initializes libvips with configured cache limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Build→Image | The build environment pulls external packages from Alpine and Go registries. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | External Dependencies | mitigate | Use go.sum and fixed Alpine versions for reproducible and secure builds. |
</threat_model>

<verification>
- Run `go build ./...` to ensure syntax is correct.
- Run `docker build -t gatuno-check .` to ensure Alpine configuration is valid.
</verification>

<success_criteria>
- Project structure exists: `cmd/`, `internal/processor/`, `pkg/`.
- `go.mod` exists with `gatuno` module name.
- `Dockerfile` produces a working Alpine image with `vips` dependencies.
- `main.go` runs and initializes `libvips` without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-dockerized-build/01-01-SUMMARY.md`
</output>
