---
phase: 01-core-engine-dockerized-build
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [internal/processor/engine.go, internal/processor/engine_test.go, internal/processor/test_utils.go]
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04]
user_setup: []

must_haves:
  truths:
    - "Images are converted to WebP lossless format"
    - "All metadata is removed from the processed image"
    - "Visual orientation is preserved via auto-rotation before stripping metadata"
    - "All processing happens in-memory with byte buffers"
  artifacts:
    - path: "internal/processor/engine.go"
      provides: "Core image transformation logic"
    - path: "internal/processor/engine_test.go"
      provides: "Verification of metadata removal and format conversion"
  key_links:
    - from: "internal/processor/engine.go"
      to: "bimg.Process"
      via: "image transformation call"
---

<objective>
Implement the core image processing engine using bimg. This engine will be responsible for stripping all metadata, auto-rotating images based on EXIF orientation, and converting the final output to lossless WebP format. All operations must occur entirely in-memory using byte buffers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-engine-dockerized-build/01-core-engine-dockerized-build-CONTEXT.md
@.planning/phases/01-core-engine-dockerized-build/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the core processing function</name>
  <files>internal/processor/engine.go</files>
  <behavior>
    - Function `Process(input []byte) ([]byte, error)`
    - If input is empty, return error.
    - Output must be in WebP format.
    - Output must have no metadata (EXIF/IPTC/XMP).
    - Auto-rotation must be applied.
  </behavior>
  <action>
    - Implement `Process` function in `internal/processor/engine.go` using `bimg`.
    - Use `bimg.Options`:
      - `Type: bimg.WEBP`
      - `Lossless: true`
      - `NoMetadata: true`
      - `AutoRotate: true`
    - Per D-04.
  </action>
  <verify>
    <automated>go build ./internal/processor</automated>
  </verify>
  <done>Core processing function implemented with bimg.</done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive unit tests for image processing</name>
  <files>internal/processor/engine_test.go, internal/processor/test_utils.go</files>
  <action>
    - Create `internal/processor/test_utils.go` to generate a small sample image (e.g., a 1x1 JPEG with EXIF orientation metadata) or load a minimal asset.
    - Implement tests in `internal/processor/engine_test.go`:
      - `TestProcess_MetadataRemoval`: Verify `bimg.Metadata(output).EXIF` is empty after processing.
      - `TestProcess_WebPConversion`: Verify output image type is WebP.
      - `TestProcess_MemoryUsage`: Ensure no large temporary files are created (if verifiable).
    - Per D-05.
  </action>
  <verify>
    <automated>go test -v ./internal/processor/...</automated>
  </verify>
  <done>Unit tests verify that metadata is stripped and images are converted to lossless WebP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Buffer Input | The processor receives raw bytes from an untrusted source. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02 | Denial of Service | Processor | mitigate | Use libvips cache limits (Wave 1) and memory-safe byte buffer operations. |
| T-01-03 | Information Disclosure | Metadata | mitigate | Explicitly set NoMetadata: true and verify in tests. |
</threat_model>

<verification>
- Run `go test ./internal/processor/...`
</verification>

<success_criteria>
- `internal/processor/engine.go` implements metadata stripping and WebP conversion.
- `go test` passes for the processor package.
- Output image type is verified as WebP in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-engine-dockerized-build/01-02-SUMMARY.md`
</output>
