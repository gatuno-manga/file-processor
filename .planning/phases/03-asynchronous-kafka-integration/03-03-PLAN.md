---
phase: 03-asynchronous-kafka-integration
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified: [internal/orchestrator/kafka.go, cmd/server/main.go, internal/orchestrator/kafka_test.go]
autonomous: false
requirements: [ASYNC-01, ASYNC-02, REL-01, CORE-04]
user_setup: []

must_haves:
  truths:
    - "Orchestrator can process a Kafka event by downloading, processing, and uploading"
    - "Main server starts both gRPC and Kafka listeners simultaneously"
    - "The full flow maintains all data in-memory (no disk writes)"
  artifacts:
    - path: "internal/orchestrator/kafka.go"
      provides: "Async orchestration logic"
    - path: "cmd/server/main.go"
      provides: "Dual-mode server entry point"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/orchestrator/kafka.go"
      via: "Function call"
      pattern: "orchestrator\\.NewKafkaOrchestrator"
---

<objective>
Coordinate the Kafka events with the storage adapter and worker pool, and update the main server entry point.

Purpose: Complete the event-driven image processing flow.
Output: Kafka orchestrator and updated main server.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-asynchronous-kafka-integration/03-asynchronous-kafka-integration-CONTEXT.md
@.planning/phases/03-asynchronous-kafka-integration/03-01-SUMMARY.md
@.planning/phases/03-asynchronous-kafka-integration/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Kafka Orchestrator</name>
  <files>internal/orchestrator/kafka.go, internal/orchestrator/kafka_test.go</files>
  <behavior>
    - Test 1: On `ImageDownloadedEvent`, download from Storage, submit to Pool, then upload result and produce `FileSanitizedEvent`.
    - Test 2: Ensure error during download doesn't crash the orchestrator.
    - Test 3: Ensure data remains in-memory throughout the entire orchestration flow.
  </behavior>
  <action>
    Create `internal/orchestrator/kafka.go` to manage the async flow (per D-11).
    It should consume from the Kafka adapter, download the image using the storage adapter, submit the image bytes to the `internal/pool`, upload the processed bytes using the storage adapter, and publish the final event.
    The orchestrator MUST use the existing worker pool (from Phase 2) to process images.
  </action>
  <verify>
    <automated>go test -v ./internal/orchestrator/...</automated>
  </verify>
  <done>Kafka orchestrator implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Update Main Server Integration</name>
  <files>cmd/server/main.go</files>
  <action>
    Modify `cmd/server/main.go` to initialize the Kafka and Storage adapters (per D-08).
    Use `golang.org/x/sync/errgroup` to run both the gRPC server and the Kafka consumer orchestrator in parallel.
    Ensure both respond to OS signals for future graceful shutdown (Phase 4).
    Wire the existing bounded worker pool into both the gRPC server and the Kafka orchestrator.
  </action>
  <verify>
    <automated>go build ./cmd/server/main.go</automated>
  </verify>
  <done>Main server integrated with Kafka and gRPC.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: End-to-End Flow Verification</name>
  <what-built>Full asynchronous image processing flow via Kafka and S3/MinIO.</what-built>
  <how-to-verify>
    1. Start a local Kafka (e.g. via Docker Compose).
    2. Start a local MinIO (e.g. via Docker Compose).
    3. Run Gatuno with Kafka and Storage env vars.
    4. Upload an image to MinIO.
    5. Produce an `image.downloaded` event to Kafka.
    6. Check Gatuno logs for processing.
    7. Verify a sanitized image exists in the output MinIO path.
    8. Check Kafka for the `file.sanitized` event.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → Pool | Interaction with internal concurrency pool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Denial of Service | Pool Saturation | mitigate | Bounded worker pool (from Phase 2) limits concurrent processing |
| T-03-03-02 | Resource Exhaustion | In-memory buffers | mitigate | Pool limit ensures maximum memory usage is predictable |
</threat_model>

<verification>
Verify full async flow: Kafka Event -> S3 Download -> Pool -> Process -> S3 Upload -> Kafka Event.
</verification>

<success_criteria>
Gatuno can process images asynchronously from Kafka events while maintaining all data in memory.
</success_criteria>

<output>
After completion, create `.planning/phases/03-asynchronous-kafka-integration/03-03-SUMMARY.md`
</output>
