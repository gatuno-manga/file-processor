---
phase: 03-asynchronous-kafka-integration
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [internal/adapter/kafka/adapter.go, internal/adapter/kafka/adapter_test.go, internal/adapter/kafka/event.go]
autonomous: true
requirements: [ASYNC-01, ASYNC-02, REL-01]
user_setup:
  - service: kafka
    why: "Messaging for async image processing"
    env_vars:
      - name: KAFKA_BROKERS
        source: "Comma-separated list of Kafka brokers"
      - name: KAFKA_TOPIC_INPUT
        source: "Topic to consume events from (e.g. image.downloaded)"
      - name: KAFKA_TOPIC_OUTPUT
        source: "Topic to publish results to (e.g. file.sanitized)"

must_haves:
  truths:
    - "Kafka adapter implements consumer and producer ports"
    - "Consumer can fetch messages from the input topic"
    - "Producer can publish events to the output topic"
  artifacts:
    - path: "internal/adapter/kafka/adapter.go"
      provides: "Kafka implementation"
    - path: "internal/adapter/kafka/event.go"
      provides: "Event structures"
  key_links:
    - from: "internal/adapter/kafka/adapter.go"
      to: "kafka-go"
      via: "SDK import"
      pattern: "github\\.com/segmentio/kafka-go"
---

<objective>
Implement the Kafka adapter to support consuming image processing jobs and publishing results.

Purpose: Provide the messaging layer for asynchronous workflows.
Output: Kafka adapter and event schema definitions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-asynchronous-kafka-integration/03-asynchronous-kafka-integration-CONTEXT.md
@.planning/phases/03-asynchronous-kafka-integration/03-asynchronous-kafka-integration-DISCOVERY.md
@.planning/phases/03-asynchronous-kafka-integration/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Event Schemas</name>
  <files>internal/adapter/kafka/event.go</files>
  <action>
    Define the `ImageDownloadedEvent` and `FileSanitizedEvent` structures as JSON.
    `ImageDownloadedEvent` should include the storage key (path to image).
    `FileSanitizedEvent` should include the new storage key of the processed image.
  </action>
  <verify>
    <automated>go build ./internal/adapter/kafka/event.go</automated>
  </verify>
  <done>Event structures defined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Kafka Adapter</name>
  <files>internal/adapter/kafka/adapter.go, internal/adapter/kafka/adapter_test.go</files>
  <behavior>
    - Test 1: Consumer reads message from Kafka and parses JSON event.
    - Test 2: Producer serializes JSON event and writes message to Kafka.
    - Test 3: Context cancellation is respected during Kafka operations.
  </behavior>
  <action>
    Implement the Kafka consumer and producer in `internal/adapter/kafka/adapter.go` using `segmentio/kafka-go` (per D-01).
    Ensure the consumer uses a worker group pattern or simple loop that can be hooked into the bounded pool later.
    Use mocks for the `kafka-go` reader/writer in unit tests.
  </action>
  <verify>
    <automated>go test -v ./internal/adapter/kafka/...</automated>
  </verify>
  <done>Kafka adapter implemented and passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → Kafka | Connection to message broker (untrusted incoming data from producer side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Kafka Message | mitigate | Use authenticated Kafka connections (SASL/SSL) |
| T-03-02-02 | Information Disclosure | Event Payload | mitigate | Ensure event payload does not contain PII or secrets |
</threat_model>

<verification>
Ensure Kafka adapter handles JSON events and respects context timeouts/cancellation.
</verification>

<success_criteria>
Kafka adapter successfully serializes/deserializes events and communicates with mock brokers in tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-asynchronous-kafka-integration/03-02-SUMMARY.md`
</output>
