---
phase: 03-asynchronous-kafka-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/port/storage.go, internal/port/kafka.go, internal/adapter/storage/s3.go, internal/adapter/storage/s3_test.go]
autonomous: true
requirements: [ASYNC-01, ASYNC-02, CORE-04]
user_setup:
  - service: minio
    why: "Object storage for async downloads/uploads"
    env_vars:
      - name: STORAGE_ENDPOINT
        source: "MinIO/S3 endpoint"
      - name: STORAGE_ACCESS_KEY
        source: "MinIO/S3 access key"
      - name: STORAGE_SECRET_KEY
        source: "MinIO/S3 secret key"
      - name: STORAGE_BUCKET
        source: "MinIO/S3 bucket name"

must_haves:
  truths:
    - "Storage port defines Download and Upload methods using byte buffers"
    - "S3 adapter can download an object from a bucket into a byte buffer in memory"
    - "S3 adapter can upload a byte buffer from memory to a bucket"
  artifacts:
    - path: "internal/port/storage.go"
      provides: "Storage interface"
    - path: "internal/port/kafka.go"
      provides: "Kafka consumer/producer interfaces"
    - path: "internal/adapter/storage/s3.go"
      provides: "S3/MinIO implementation"
  key_links:
    - from: "internal/adapter/storage/s3.go"
      to: "minio-go"
      via: "SDK import"
      pattern: "github\\.com/minio/minio-go"
---

<objective>
Define internal ports for storage and messaging, and implement the S3/MinIO storage adapter.

Purpose: Enable asynchronous workflows to download/upload image data to external storage.
Output: Storage port, Kafka port, and S3 adapter.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-asynchronous-kafka-integration/03-asynchronous-kafka-integration-CONTEXT.md
@.planning/phases/03-asynchronous-kafka-integration/03-asynchronous-kafka-integration-DISCOVERY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Port Interfaces</name>
  <files>internal/port/storage.go, internal/port/kafka.go</files>
  <action>
    Define the Storage interface in `internal/port/storage.go` with `Download(ctx, key) ([]byte, error)` and `Upload(ctx, key, data) error`.
    Define the Kafka interfaces in `internal/port/kafka.go` for Consumer and Producer.
    Ensure all methods accept `context.Context` (per D-07).
  </action>
  <verify>
    <automated>go build ./internal/port/...</automated>
  </verify>
  <done>Ports defined and buildable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement S3/MinIO Adapter</name>
  <files>internal/adapter/storage/s3.go, internal/adapter/storage/s3_test.go</files>
  <behavior>
    - Test 1: Download from S3-compatible storage into memory.
    - Test 2: Upload from memory to S3-compatible storage.
    - Both should use byte buffers only, respecting CORE-04.
  </behavior>
  <action>
    Implement the Storage interface using `minio-go/v7` in `internal/adapter/storage/s3.go`.
    Add unit tests in `internal/adapter/storage/s3_test.go` using a mock storage client or MinIO test container if feasible, otherwise mock the `minio.Client`.
  </action>
  <verify>
    <automated>go test -v ./internal/adapter/storage/...</automated>
  </verify>
  <done>S3 adapter implemented and passing tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service → S3/MinIO | Outbound connection to object storage (trusted endpoint via secrets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | S3 Upload | mitigate | Ensure TLS for all S3/MinIO connections |
| T-03-01-02 | Information Disclosure | S3 Credentials | mitigate | Use environment variables for secrets, never log them |
</threat_model>

<verification>
Ensure storage adapter can perform in-memory operations as required by CORE-04.
</verification>

<success_criteria>
Storage and Kafka ports defined. S3 adapter implemented with passing unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-asynchronous-kafka-integration/03-01-SUMMARY.md`
</output>
