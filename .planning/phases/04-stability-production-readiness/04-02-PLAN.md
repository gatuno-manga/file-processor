---
phase: 04-stability-production-readiness
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified: [cmd/server/main.go, internal/api/grpc/server.go, internal/adapter/kafka/adapter.go, internal/pool/pool.go]
autonomous: true
requirements: [REL-02, MON-02]
must_haves:
  truths:
    - "/healthz returns 200 immediately"
    - "/readyz returns 200 only if Kafka and gRPC are healthy"
    - "Application waits for in-flight jobs to finish upon SIGTERM"
  artifacts:
    - path: "cmd/server/main.go"
      provides: "HTTP health server and graceful shutdown orchestration"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/pool/pool.go"
      via: "pool.Stop() call during shutdown"
---

<objective>
Implement health probes and ensure all components stop cleanly during shutdown.

Purpose: Enable orchestration (like Kubernetes) to monitor service health and ensure no data loss during updates.
Output: HTTP health server with probes, readiness logic for gRPC/Kafka, and sequenced graceful shutdown.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stability-production-readiness/04-stability-production-readiness-CONTEXT.md
@cmd/server/main.go
@internal/pool/pool.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HTTP health check server</name>
  <files>cmd/server/main.go</files>
  <action>
    - Using `net/http`, start a separate goroutine in `main.go` that listens on `HealthPort` (from config).
    - Implement `/healthz` (liveness) returning HTTP 200 immediately.
    - Implement `/readyz` (readiness) that calls a composite check.
    - Reference: D-01.
  </action>
  <verify>
    <automated>curl localhost:8081/healthz should return 200; curl localhost:8081/readyz should return 200.</automated>
  </verify>
  <done>HTTP health server is active with basic probes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Readiness logic for Kafka and gRPC</name>
  <files>internal/api/grpc/server.go, internal/adapter/kafka/adapter.go, cmd/server/main.go</files>
  <action>
    - Add a `Ready() bool` method to `internal/adapter/kafka/KafkaAdapter` (e.g., check if connection to broker is alive).
    - Add a `Ready() bool` method to `internal/api/grpc/Server`.
    - Wire these into the `/readyz` handler in `main.go`.
    - Reference: D-01.
  </action>
  <verify>
    <automated>Check readiness endpoint logs when Kafka is disconnected vs connected.</automated>
  </verify>
  <done>Readiness probe accurately reflects component availability.</done>
</task>

<task type="auto">
  <name>Task 3: Refine main.go shutdown sequence</name>
  <files>cmd/server/main.go, internal/pool/pool.go</files>
  <action>
    - In `internal/pool/pool.go`, add a `Stop()` function that closes the `jobChan` and waits for all workers to finish.
    - In `main.go`, update the signal handler/context cancelation to:
      1. Stop the Kafka consumer (cancel context passed to `Consume`).
      2. Call `pool.Stop()` to finish current image processing tasks.
      3. Call `grpcServer.GracefulStop()` to finish active RPCs.
    - Ensure a final timeout (e.g., 30s) after which the process is forced to exit.
    - Reference: D-05.
  </action>
  <verify>
    <automated>Send SIGTERM to the process while jobs are running and verify they finish before exit.</automated>
  </verify>
  <done>Sequenced graceful shutdown is implemented, protecting in-flight work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Monitoring: Probes exposed via HTTP.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | health server | mitigate | Use a separate port for health checks and keep handlers minimal. |
| T-04-02-02 | Information Disclosure | health server | mitigate | Do not return detailed error messages in health check responses; just HTTP 200/503. |
</threat_model>

<verification>
- Start the app, verify `/healthz` and `/readyz` endpoints.
- Stop Kafka and verify `/readyz` returns 503.
- Trigger SIGTERM and verify logs show the sequenced shutdown.
</verification>

<success_criteria>
- The service responds to liveness and readiness probes.
- Readiness probe fails if Kafka or gRPC is unhealthy.
- SIGTERM results in a clean exit after in-flight jobs are completed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stability-production-readiness/04-02-SUMMARY.md`
</output>
