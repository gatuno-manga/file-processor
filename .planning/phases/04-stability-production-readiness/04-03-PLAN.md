---
phase: 04-stability-production-readiness
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified: [internal/processor/engine.go, internal/pool/pool.go, cmd/server/main.go, Dockerfile]
autonomous: true
requirements: [MON-01, ENV-04, REL-04, REL-05, CORE-04]
must_haves:
  truths:
    - "/metrics endpoint returns Prometheus metrics"
    - "Container runs as a non-root user"
    - "Image processing occurs entirely in-memory under load"
  artifacts:
    - path: "Dockerfile"
      provides: "Hardened production-ready container"
  key_links:
    - from: "internal/processor/engine.go"
      to: "prometheus"
      via: "Histogram and Counter instrumentation"
---

<objective>
Implement telemetry with Prometheus and harden the production container for deployment.

Purpose: Provide deep observability into processing performance and ensure the container is secure and efficient.
Output: Prometheus metrics endpoint, hardened Dockerfile, and stability verification tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stability-production-readiness/04-stability-production-readiness-CONTEXT.md
@internal/processor/engine.go
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Prometheus metrics</name>
  <files>internal/processor/engine.go, internal/pool/pool.go, cmd/server/main.go</files>
  <action>
    - Add `prometheus` client dependencies.
    - Define and register metrics:
      - `gatuno_image_processing_seconds`: Histogram for processing time.
      - `gatuno_image_processed_total`: Counter for total images processed (labeled by status: success/error).
      - `gatuno_worker_pool_active_workers`: Gauge for current pool utilization.
    - Instrument `internal/processor/engine.go` and `internal/pool/pool.go`.
    - Serve metrics on `/metrics` via the health server (on the health port).
    - Reference: D-04.
  </action>
  <verify>
    <automated>curl localhost:8081/metrics should return Prometheus-formatted metrics.</automated>
  </verify>
  <done>Service observability is enhanced with detailed processing metrics.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Dockerfile for Production</name>
  <files>Dockerfile</files>
  <action>
    - Update `Dockerfile` to use a non-root user (`appuser`).
    - Use multi-stage builds more effectively (builder vs runtime).
    - Ensure `vips` dependencies are correctly pinned to minimal runtime versions.
    - Set `USER appuser` and ensure correct ownership of the binary and necessary directories.
    - Reference: D-11.
  </action>
  <verify>
    <automated>Build the Docker image and run `docker run gatuno id` to verify it's running as non-root.</automated>
  </verify>
  <done>Production-ready Docker container is security-hardened and optimized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stability verification and in-memory check</name>
  <files>internal/processor/engine_test.go, cmd/server/main_test.go</files>
  <behavior>
    - Verify that no temporary files are created during processing (using `/proc/self/fd` or similar, or just ensuring no disk-writing code exists).
    - Ensure `pool.Stop()` correctly drains all pending jobs.
    - Check memory usage stability under a sustained load of mocked processing jobs.
    - Per D-08, D-09, D-10.
  </behavior>
  <action>
    - Add unit/integration tests for health check handlers and shutdown sequence.
    - Run `go test ./...` with high concurrency using `testing.B` to ensure no race conditions or leaks.
  </action>
  <verify>
    <automated>go test -v -race ./...</automated>
  </verify>
  <done>Service stability is verified under load, and in-memory processing is confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Monitoring: Prometheus `/metrics` endpoint.
- Container: Host isolation for the running process.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Information Disclosure | metrics | mitigate | Do not include sensitive information in metric labels. |
| T-04-03-02 | Privilege Escalation | container | mitigate | Running as non-root user in the container significantly reduces the impact of a process compromise. |
| T-04-03-03 | Denial of Service | metrics | mitigate | Ensure metrics collection is efficient and does not impact main processing latency. |
</threat_model>

<verification>
- Verify metrics are correctly collected and exported.
- Verify the Docker image runs as a non-privileged user.
- Verify tests pass and confirm stable behavior under high concurrency.
</verification>

<success_criteria>
- Metrics are available on `/metrics`.
- Docker image is optimized and runs as non-root.
- All tests (stability and functional) pass without race conditions.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stability-production-readiness/04-03-SUMMARY.md`
</output>
