---
phase: 04-stability-production-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/processor/config.go, cmd/server/main.go, internal/adapter/kafka/adapter.go, internal/orchestrator/kafka.go, internal/pool/pool.go]
autonomous: true
requirements: [REL-02, REL-03, ENV-03, MON-03]
must_haves:
  truths:
    - "Application logs are in JSON format when APP_ENV=production"
    - "Configuration can be overridden via environment variables"
    - "libvips cache limits are applied at startup"
  artifacts:
    - path: "internal/processor/config.go"
      provides: "Centralized configuration struct and loader"
  key_links:
    - from: "cmd/server/main.go"
      to: "internal/processor/config.go"
      via: "LoadConfig call"
---

<objective>
Finalize configuration logic and replace standard logging with structured `slog`.

Purpose: Ensure the application has production-grade observability and flexible, centralized configuration.
Output: Centralized `Config` struct, `slog` integration across all layers, and refined `libvips` cache limits.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-stability-production-readiness/04-stability-production-readiness-CONTEXT.md
@internal/processor/config.go
@cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all logging to slog</name>
  <files>cmd/server/main.go, internal/adapter/kafka/adapter.go, internal/orchestrator/kafka.go, internal/pool/pool.go</files>
  <action>
    - Import "log/slog" and replace all usage of "log" and "fmt.Printf" (used for logging) with `slog.Info`, `slog.Error`, etc.
    - In `main.go`, initialize the default logger: use `slog.NewJSONHandler` if an environment variable `APP_ENV=production` is set, otherwise use `slog.NewTextHandler`.
    - Ensure structured fields are used (e.g., `slog.Info("msg", "key", val)` instead of `fmt.Printf`).
    - Per D-03, D-07.
  </action>
  <verify>
    <automated>grep -r "log\." . | grep -v "slog" should return no results in main logic files; app starts and logs in structured format.</automated>
  </verify>
  <done>All standard logging is replaced with structured `slog` output.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Config struct and environment loader</name>
  <files>internal/processor/config.go</files>
  <action>
    - Define a `Config` struct that captures all necessary environment variables:
      - `Port` (gRPC), `HealthPort`, `PoolSize`
      - `KafkaBrokers`, `KafkaInputTopic`, `KafkaOutputTopic`
      - `StorageEndpoint`, `StorageAccessKey`, `StorageSecretKey`, `StorageSSL`
      - `VipsMaxCache`, `VipsMaxCacheMem`
    - Implement a `LoadConfig()` function that reads these from the environment using `os.Getenv` and provides sensible defaults.
    - Reference: D-06.
  </action>
  <verify>
    <automated>go test -v internal/processor/config_test.go (must create this test to verify defaults and override logic)</automated>
  </verify>
  <done>Centralized configuration management is implemented with defaults.</done>
</task>

<task type="auto">
  <name>Task 3: Refine libvips cache initialization</name>
  <files>internal/processor/config.go, cmd/server/main.go</files>
  <action>
    - Update `InitVips()` to accept the `Config` struct.
    - Use `bimg.VipsCacheSetMax(cfg.VipsMaxCache)` and `bimg.VipsCacheSetMaxMem(cfg.VipsMaxCacheMem)`.
    - Update `main.go` to load the config first and pass it to `InitVips`.
    - Reference: D-02.
  </action>
  <verify>
    <automated>Check server startup logs to confirm Vips initialized with config values.</automated>
  </verify>
  <done>Libvips cache limits are explicitly configurable and initialized correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Environment: Configuration via environment variables.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Information Disclosure | logging | mitigate | Use `slog` to ensure sensitive data (like S3 keys) are NOT logged in structured logs. |
| T-04-01-02 | Denial of Service | config | mitigate | Validate `VipsMaxCacheMem` to ensure it's within reasonable bounds to prevent intentional OOM through config. |
</threat_model>

<verification>
- Start the server and verify logs are JSON-formatted (if APP_ENV=production).
- Verify all configuration parameters are correctly read from environment variables.
</verification>

<success_criteria>
- The application uses `slog` exclusively for logging.
- All configuration is centralized in `internal/processor/config.go`.
- `libvips` cache limits are successfully set from configuration.
</success_criteria>

<output>
After completion, create `.planning/phases/04-stability-production-readiness/04-01-SUMMARY.md`
</output>
