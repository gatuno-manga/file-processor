---
phase: 02-synchronous-api-bounded-concurrency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [proto/v1/processor.proto, internal/api/grpc/pb/processor.pb.go, internal/api/grpc/pb/processor_grpc.pb.go]
autonomous: true
requirements: [SYNC-01]

must_haves:
  truths:
    - "gRPC service definition exists in proto/v1/processor.proto"
    - "Go gRPC code is generated from the proto definition"
  artifacts:
    - path: "proto/v1/processor.proto"
      provides: "gRPC contract"
    - path: "internal/api/grpc/pb/processor.pb.go"
      provides: "Generated Protobuf messages"
    - path: "internal/api/grpc/pb/processor_grpc.pb.go"
      provides: "Generated gRPC service/client"
  key_links:
    - from: "proto/v1/processor.proto"
      to: "internal/api/grpc/pb"
      via: "protoc generation"
---

<objective>
Define the gRPC service contract and generate the corresponding Go code to allow typed communication for image processing tasks.

Purpose: Establishes the interface for the synchronous API.
Output: .proto file and generated Go packages.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-synchronous-api-bounded-concurrency/02-synchronous-api-bounded-concurrency-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define gRPC service in proto/v1/processor.proto</name>
  <files>proto/v1/processor.proto</files>
  <action>
    Create a Protobuf file defining the ImageProcessor service.
    - Package name: gatuno.v1
    - Go package option: github.com/luis/file-processor/internal/api/grpc/pb
    - Request message: ProcessRequest (contains image bytes)
    - Response message: ProcessResponse (contains processed image bytes)
    - Service method: Process (unary RPC)
  </action>
  <verify>
    <automated>grep "service ImageProcessor" proto/v1/processor.proto</automated>
  </verify>
  <done>Protobuf contract defined with Process unary RPC.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Go gRPC code using protoc</name>
  <files>internal/api/grpc/pb/processor.pb.go, internal/api/grpc/pb/processor_grpc.pb.go</files>
  <action>
    1. Ensure necessary directories exist.
    2. Install/verify protoc-gen-go and protoc-gen-go-grpc.
    3. Run protoc to generate Go code in internal/api/grpc/pb.
    Command pattern: protoc --go_out=. --go_opt=paths=source_relative --go-grpc_out=. --go-grpc_opt=paths=source_relative proto/v1/processor.proto
  </action>
  <verify>
    <automated>ls internal/api/grpc/pb/*.pb.go</automated>
  </verify>
  <done>Generated Go files exist and are ready for implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → API | Untrusted image bytes cross this boundary via gRPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Protobuf payload | mitigate | Protobuf provides structure; bimg/libvips will validate image headers |
| T-02-02 | DoS | Large images | mitigate | (Planned) Bounded worker pool will limit concurrent processing |
</threat_model>

<verification>
Check if internal/api/grpc/pb/processor_grpc.pb.go contains the ImageProcessorServer interface.
</verification>

<success_criteria>
- proto/v1/processor.proto defines Gatuno's API.
- go build ./internal/api/grpc/pb passes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-synchronous-api-bounded-concurrency/02-01-SUMMARY.md`
</output>
