---
phase: 02-synchronous-api-bounded-concurrency
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified: [internal/api/grpc/server.go, internal/api/grpc/server_test.go, cmd/server/main.go]
autonomous: true
requirements: [SYNC-02, REL-01]

must_haves:
  truths:
    - "gRPC server processes ProcessRequest by calling the worker pool"
    - "Server respects context deadline from gRPC requests"
    - "Server successfully starts and listens on a configured port"
  artifacts:
    - path: "internal/api/grpc/server.go"
      provides: "gRPC server implementation"
    - path: "cmd/server/main.go"
      provides: "Entry point with pool/server initialization"
  key_links:
    - from: "internal/api/grpc/server.go"
      to: "internal/pool"
      via: "calls pool.Process"
---

<objective>
Implement the gRPC server handler and integrate it into the main application lifecycle, ensuring it uses the bounded worker pool and respects timeouts.

Purpose: Provide the public interface for the image processing service.
Output: Working gRPC server implementation and updated main.go.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-synchronous-api-bounded-concurrency/02-synchronous-api-bounded-concurrency-CONTEXT.md
@internal/api/grpc/pb/processor_grpc.pb.go
@internal/pool/pool.go
@cmd/server/main.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement gRPC server in internal/api/grpc/server.go</name>
  <files>internal/api/grpc/server.go</files>
  <action>
    Implement the ImageProcessorServer interface generated in Plan 01.
    - Add a `Server` struct that holds a reference to the `Pool` from Plan 02.
    - Implement `Process(ctx, req) (*pb.ProcessResponse, error)`.
    - Ensure it passes the incoming gRPC `ctx` to the `pool.Process` call.
    - Map internal errors to gRPC status codes (e.g., StatusInternalError).
  </action>
  <verify>
    <automated>go build ./internal/api/grpc/...</automated>
  </verify>
  <done>gRPC server handler implemented and uses worker pool.</done>
</task>

<task type="auto">
  <name>Task 2: Update cmd/server/main.go to start gRPC server</name>
  <files>cmd/server/main.go</files>
  <action>
    Initialize the worker pool and start the gRPC listener.
    - Read WORKER_POOL_SIZE from environment (default to 0 -> GOMAXPROCS).
    - Read GRPC_PORT from environment (default to 50051).
    - Call processor.InitVips().
    - Start worker pool.
    - Create gRPC server, register ImageProcessorServer, and ListenAndServe on the configured port.
    - Ensure it blocks or uses a goroutine with an error channel (basic for now).
  </action>
  <verify>
    <automated>go build ./cmd/server && ls ./server</automated>
  </verify>
  <done>main.go wired to start the gRPC service on port 50051.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for gRPC server</name>
  <files>internal/api/grpc/server_test.go</files>
  <action>
    Add a test using `google.golang.org/grpc/test/bufconn` to verify server logic.
    - Test successful image processing through the gRPC server and mock/real pool.
    - Test context cancellation propagation.
    - Test error handling for empty input.
  </action>
  <verify>
    <automated>go test ./internal/api/grpc/...</automated>
  </verify>
  <done>gRPC server logic verified by unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → API | gRPC endpoint exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | DoS | gRPC Listener | mitigate | Context-aware timeouts and bounded pool prevent stalling threads |
</threat_model>

<verification>
Start the server and use `grpcurl` (if available) or a test client to send a small image and verify a WebP response.
</verification>

<success_criteria>
- gRPC server correctly processes images via the worker pool.
- ProcessRequest → Pool → ProcessResponse flow works.
- Timeouts work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-synchronous-api-bounded-concurrency/02-03-SUMMARY.md`
</output>
